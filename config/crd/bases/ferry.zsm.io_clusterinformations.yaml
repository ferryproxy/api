
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.4.1
  creationTimestamp: null
  name: clusterinformations.ferry.zsm.io
spec:
  group: ferry.zsm.io
  names:
    kind: ClusterInformation
    listKind: ClusterInformationList
    plural: clusterinformations
    singular: clusterinformation
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.exportedTo
      name: exported-to
      type: string
    - jsonPath: .status.importedFrom
      name: imported-from
      type: string
    - jsonPath: .status.mode
      name: mode
      type: string
    - jsonPath: .status.lastSynchronizationTimestamp
      name: last-synchronization
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: ClusterInformation is the Schema for the clusterinformations
          API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: ClusterInformationSpec defines the desired state of ClusterInformation
            properties:
              egress:
                description: Egress is the egress of the cluster.
                properties:
                  defaultProxies:
                    description: DefaultProxies is a default list of proxies to use
                      for the route
                    items:
                      description: Proxy defines the desired state of ClusterInformationSpecRoute
                      properties:
                        clusterName:
                          description: ClusterName is the name of the cluster to proxy
                            to. cannot be specified together with Proxy.
                          type: string
                        proxy:
                          description: Proxy is the proxy to use. cannot be specified
                            together with ClusterName.
                          type: string
                      type: object
                    type: array
                  ip:
                    description: IP is the IP address to expose. cannot be specified
                      together with ServiceName and ServiceNamespace.
                    type: string
                  port:
                    description: Port is the port to expose.
                    format: int32
                    type: integer
                  proxies:
                    additionalProperties:
                      description: Proxies defines the desired state of ClusterInformationSpecRoute
                      items:
                        description: Proxy defines the desired state of ClusterInformationSpecRoute
                        properties:
                          clusterName:
                            description: ClusterName is the name of the cluster to
                              proxy to. cannot be specified together with Proxy.
                            type: string
                          proxy:
                            description: Proxy is the proxy to use. cannot be specified
                              together with ClusterName.
                            type: string
                        type: object
                      type: array
                    description: Proxies is a list of proxies to use for the route
                    type: object
                  serviceName:
                    description: ServiceName is the name of the service to expose.
                      cannot be specified together with IP.
                    type: string
                  serviceNamespace:
                    description: ServiceNamespace is the namespace of the service
                      to expose. cannot be specified together with IP.
                    type: string
                type: object
              ingress:
                description: Ingress is the ingress of the cluster.
                properties:
                  defaultProxies:
                    description: DefaultProxies is a default list of proxies to use
                      for the route
                    items:
                      description: Proxy defines the desired state of ClusterInformationSpecRoute
                      properties:
                        clusterName:
                          description: ClusterName is the name of the cluster to proxy
                            to. cannot be specified together with Proxy.
                          type: string
                        proxy:
                          description: Proxy is the proxy to use. cannot be specified
                            together with ClusterName.
                          type: string
                      type: object
                    type: array
                  ip:
                    description: IP is the IP address to expose. cannot be specified
                      together with ServiceName and ServiceNamespace.
                    type: string
                  port:
                    description: Port is the port to expose.
                    format: int32
                    type: integer
                  proxies:
                    additionalProperties:
                      description: Proxies defines the desired state of ClusterInformationSpecRoute
                      items:
                        description: Proxy defines the desired state of ClusterInformationSpecRoute
                        properties:
                          clusterName:
                            description: ClusterName is the name of the cluster to
                              proxy to. cannot be specified together with Proxy.
                            type: string
                          proxy:
                            description: Proxy is the proxy to use. cannot be specified
                              together with ClusterName.
                            type: string
                        type: object
                      type: array
                    description: Proxies is a list of proxies to use for the route
                    type: object
                  serviceName:
                    description: ServiceName is the name of the service to expose.
                      cannot be specified together with IP.
                    type: string
                  serviceNamespace:
                    description: ServiceNamespace is the namespace of the service
                      to expose. cannot be specified together with IP.
                    type: string
                type: object
              kubeconfig:
                description: Kubeconfig is the kubeconfig of the cluster, cannot be
                  specified together with InCluster.
                format: byte
                type: string
            type: object
          status:
            description: ClusterInformationStatus defines the observed state of ClusterInformation
            properties:
              conditions:
                description: Conditions current service state
                items:
                  description: "Condition contains details for one aspect of the current
                    state of this API Resource. --- This struct is intended for direct
                    use as an array at the field path .status.conditions.  For example,
                    type FooStatus struct{     // Represents the observations of a
                    foo's current state.     // Known .status.conditions.type are:
                    \"Available\", \"Progressing\", and \"Degraded\"     // +patchMergeKey=type
                    \    // +patchStrategy=merge     // +listType=map     // +listMapKey=type
                    \    Conditions []metav1.Condition `json:\"conditions,omitempty\"
                    patchStrategy:\"merge\" patchMergeKey:\"type\" protobuf:\"bytes,1,rep,name=conditions\"`
                    \n     // other fields }"
                  properties:
                    lastTransitionTime:
                      description: lastTransitionTime is the last time the condition
                        transitioned from one status to another. This should be when
                        the underlying condition changed.  If that is not known, then
                        using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: message is a human readable message indicating
                        details about the transition. This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: observedGeneration represents the .metadata.generation
                        that the condition was set based upon. For instance, if .metadata.generation
                        is currently 12, but the .status.conditions[x].observedGeneration
                        is 9, the condition is out of date with respect to the current
                        state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: reason contains a programmatic identifier indicating
                        the reason for the condition's last transition. Producers
                        of specific condition types may define expected values and
                        meanings for this field, and whether the values are considered
                        a guaranteed API. The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                        --- Many .condition.type values are consistent across resources
                        like Available, but because arbitrary conditions can be useful
                        (see .node.status.conditions), the ability to deconflict is
                        important. The regex it matches is (dns1123SubdomainFmt/)?(qualifiedNameFmt)
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              exportedTo:
                description: ExportedTo is the list of the cluster information exported
                  to.
                items:
                  type: string
                type: array
              importedFrom:
                description: ImportedFrom is the list of the cluster information imported
                  from.
                items:
                  type: string
                type: array
              lastSynchronizationTimestamp:
                description: LastSynchronizationTimestamp is the last time synchronization
                  to the cluster.
                format: date-time
                type: string
              mode:
                description: Mode is the mode of the cluster information.
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: []
  storedVersions: []
